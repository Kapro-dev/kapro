{{- if .Values.webhook.enabled }}
---
# Service for the admission webhook server.
# The kube-apiserver calls this Service; it must be in the same cluster as the operator.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kapro-operator.fullname" . }}-webhook
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kapro-operator.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "kapro-operator.selectorLabels" . | nindent 4 }}
  ports:
    - name: webhook
      port: 443
      targetPort: {{ .Values.webhook.port }}
---
# ValidatingWebhookConfiguration — enforces CRD invariants at admission time.
# caBundle is injected by cert-manager (cert-manager.io/inject-ca-from annotation)
# or set manually when certmanager.enabled=false.
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "kapro-operator.fullname" . }}
  labels:
    {{- include "kapro-operator.labels" . | nindent 4 }}
  {{- if .Values.webhook.certManager.enabled }}
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "kapro-operator.fullname" . }}-webhook-cert
  {{- end }}
webhooks:
  - name: validate.fleetcluster.kapro.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    matchPolicy: Equivalent
    rules:
      - apiGroups: ["kapro.io"]
        apiVersions: ["v1alpha1"]
        resources: ["fleetclusters"]
        operations: ["CREATE", "UPDATE"]
    clientConfig:
      service:
        namespace: {{ .Release.Namespace }}
        name: {{ include "kapro-operator.fullname" . }}-webhook
        path: /validate-kapro-io-v1alpha1-fleetcluster
        port: 443
      {{- if not .Values.webhook.certManager.enabled }}
      caBundle: {{ .Values.webhook.caBundle | default "" | quote }}
      {{- end }}
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values: [kube-system]

  - name: validate.promotionrun.kapro.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    matchPolicy: Equivalent
    rules:
      - apiGroups: ["kapro.io"]
        apiVersions: ["v1alpha1"]
        resources: ["promotionruns"]
        operations: ["CREATE", "UPDATE"]
    clientConfig:
      service:
        namespace: {{ .Release.Namespace }}
        name: {{ include "kapro-operator.fullname" . }}-webhook
        path: /validate-kapro-io-v1alpha1-promotionrun
        port: 443
      {{- if not .Values.webhook.certManager.enabled }}
      caBundle: {{ .Values.webhook.caBundle | default "" | quote }}
      {{- end }}
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values: [kube-system]

  - name: validate.promotionplan.kapro.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    matchPolicy: Equivalent
    rules:
      - apiGroups: ["kapro.io"]
        apiVersions: ["v1alpha1"]
        resources: ["promotionplans"]
        operations: ["CREATE", "UPDATE"]
    clientConfig:
      service:
        namespace: {{ .Release.Namespace }}
        name: {{ include "kapro-operator.fullname" . }}-webhook
        path: /validate-kapro-io-v1alpha1-promotionplan
        port: 443
      {{- if not .Values.webhook.certManager.enabled }}
      caBundle: {{ .Values.webhook.caBundle | default "" | quote }}
      {{- end }}
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values: [kube-system]

  - name: validate.approval.kapro.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    matchPolicy: Equivalent
    rules:
      - apiGroups: ["kapro.io"]
        apiVersions: ["v1alpha1"]
        resources: ["approvals"]
        operations: ["CREATE", "UPDATE"]
    clientConfig:
      service:
        namespace: {{ .Release.Namespace }}
        name: {{ include "kapro-operator.fullname" . }}-webhook
        path: /validate-kapro-io-v1alpha1-approval
        port: 443
      {{- if not .Values.webhook.certManager.enabled }}
      caBundle: {{ .Values.webhook.caBundle | default "" | quote }}
      {{- end }}

  - name: validate.promotiontrigger.kapro.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    matchPolicy: Equivalent
    rules:
      - apiGroups: ["kapro.io"]
        apiVersions: ["v1alpha1"]
        resources: ["promotiontriggers"]
        operations: ["CREATE", "UPDATE"]
    clientConfig:
      service:
        namespace: {{ .Release.Namespace }}
        name: {{ include "kapro-operator.fullname" . }}-webhook
        path: /validate-kapro-io-v1alpha1-promotiontrigger
        port: 443
      {{- if not .Values.webhook.certManager.enabled }}
      caBundle: {{ .Values.webhook.caBundle | default "" | quote }}
      {{- end }}
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values: [kube-system]
---
# MutatingWebhookConfiguration — applies defaulting and managed-label patches.
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ include "kapro-operator.fullname" . }}
  labels:
    {{- include "kapro-operator.labels" . | nindent 4 }}
  {{- if .Values.webhook.certManager.enabled }}
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "kapro-operator.fullname" . }}-webhook-cert
  {{- end }}
webhooks:
  - name: mutate.fleetcluster.kapro.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    matchPolicy: Equivalent
    rules:
      - apiGroups: ["kapro.io"]
        apiVersions: ["v1alpha1"]
        resources: ["fleetclusters"]
        operations: ["CREATE", "UPDATE"]
    clientConfig:
      service:
        namespace: {{ .Release.Namespace }}
        name: {{ include "kapro-operator.fullname" . }}-webhook
        path: /mutate-kapro-io-v1alpha1-fleetcluster
        port: 443
      {{- if not .Values.webhook.certManager.enabled }}
      caBundle: {{ .Values.webhook.caBundle | default "" | quote }}
      {{- end }}
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values: [kube-system]

  - name: mutate.approval.kapro.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: Ignore
    matchPolicy: Equivalent
    rules:
      - apiGroups: ["kapro.io"]
        apiVersions: ["v1alpha1"]
        resources: ["approvals"]
        operations: ["CREATE", "UPDATE"]
    clientConfig:
      service:
        namespace: {{ .Release.Namespace }}
        name: {{ include "kapro-operator.fullname" . }}-webhook
        path: /mutate-kapro-io-v1alpha1-approval
        port: 443
      {{- if not .Values.webhook.certManager.enabled }}
      caBundle: {{ .Values.webhook.caBundle | default "" | quote }}
      {{- end }}
{{- end }}
