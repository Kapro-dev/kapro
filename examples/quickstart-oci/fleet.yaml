# Apply after substrates/oci.yaml, deliveryunit.yaml, and plan.yaml.
# Replace ghcr.io/example with registries your spokes can reach.
apiVersion: kapro.io/v1alpha1
kind: Fleet
metadata:
  name: checkout-oci
  labels:
    kapro.io/unit: checkout-oci
    kapro.io/team: platform
    app.kubernetes.io/name: checkout
spec:
  delivery:
    mode: pull
    substrateRef: oci
    parameters:
      repository: ghcr.io/example/checkout/{appKey}
      tag: "{version}"
      auth: anonymous
  clusters:
    - name: checkout-oci-canary
      labels:
        kapro.io/stage: canary
        kapro.io/region: eu
        kapro.io/quickstart: oci
    - name: checkout-oci-production
      labels:
        kapro.io/stage: production
        kapro.io/region: eu
        kapro.io/quickstart: oci
