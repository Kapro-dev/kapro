apiVersion: kapro.io/v1alpha1
kind: DeliveryUnit
metadata:
  name: checkout-argo
  labels:
    kapro.io/unit: checkout-argo
    kapro.io/team: platform
    app.kubernetes.io/name: checkout
spec:
  defaultFleetRef: checkout-argo
  defaultPlanRef: checkout-argo
  source:
    substrateRef: argo
    units:
      - name: checkout
        substrateKind: Application
        namespace: argocd
        versionField: spec.source.targetRevision
        version: v1.2.3
